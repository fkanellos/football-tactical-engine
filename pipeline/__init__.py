"""football-tactical-engine pipeline package.

Holds the Phase 4/5 layers (`patterns`, `recommendations`, `scouting`) and the
tracking-only match-event inference layer (`events`, /docs/event-inference-design.md);
the FastAPI service wrapping stages 1-3 will join later. See /docs/phase4-5-design.md.
"""
